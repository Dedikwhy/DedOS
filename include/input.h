#ifndef INPUT_H
#define INPUT_H

// Опрашивает контроллер 8042 и раскладывает байты по драйверам клавиатуры и мыши.
// Прерываний в DedOS нет, поэтому вызывать нужно постоянно (главный цикл GUI).
void input_poll(void);

#endif
