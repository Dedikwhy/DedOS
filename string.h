#ifndef STRING_H
#define STRING_H

// Сравнение двух строк: возвращает 0, если они абсолютно одинаковы
int strcmp(const char *s1, const char *s2);

// Длина строки
int strlen(const char *str);

#endif